-- Limpeza da regra antiga de inventario.
--
-- Executar depois de fazer deploy do codigo que ja nao usa:
-- - stocks.surplus_threshold
-- - stocks.max_quantity
--
-- A coluna max_quantity pode nem existir se o rollback ja tiver sido feito.
-- O IF EXISTS torna o comando seguro.

ALTER TABLE stocks
DROP COLUMN IF EXISTS max_quantity;

ALTER TABLE stocks
DROP COLUMN IF EXISTS surplus_threshold;
